{
  "name": "hwil-private-server",
  "version": "0.7.0",
  "private": true,
  "description": "Clean-room private server, replacement content pack, and protocol diagnostic gateway for Hot Wheels Infinite Loop 1.35.0.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:data": "node tools/build-replacement-data.mjs",
    "start": "node src/index.js",
    "test": "node tools/build-replacement-data.mjs && node --test",
    "check": "node --check src/index.js && node --check src/server.js && node --check tools/build-replacement-data.mjs && node tools/build-replacement-data.mjs && node --test"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
